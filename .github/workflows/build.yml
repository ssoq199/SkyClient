name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 解压并整理文件结构
        run: |
跑[主要]
#解压 zip
unzip -o ModuleSrc.zip -d temp
# 查看解压后的结构（调试用）
ls -la temp/
#如果存在嵌套的模块是你的，你是
unzip -o ModuleSrc.zip -d temp[-d"temp/ModuleSrc"]；然后
其他的
cp -r temp/* .
请将下面的文本准确无误地翻译成
#显示最终结构：[-d[temp/ModuleSrc]
cp -r temp/* .

      - 请将下面的文本准确无误地翻译成: 列出详细文件信息（长格式）[17]
-名称：you you JDK 17: actions/setup-java@v4
Javata-tag版本：
          java-version: '17'
分发：“temuurin”: 'temurin'

      - -名称：我喜欢Android SDK: # 显示最终结构
用途:android-actions/setup-android@v3: android-actions/setup-android@v3

      - -名称：我喜欢: 运行：|
将 gradlew you you:chmod+x gradlew: |
/gradlew汇编调试-
-名称：我喜欢

      - 用途：actions/mayor-artific@v4
名称：应用程序
路径:app/build/outputs/apk/debug/*. apk:
#查看解压后的结构（you you）：ls-la temp/：app-debug
#如果存在嵌套的模块是你的，mayor:actions/mayor:app/build/outputs/apk/debug/*. apk
